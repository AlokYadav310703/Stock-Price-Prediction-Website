# ── Core API ─────────────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.31.0
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1

# ── Database ─────────────────────────────────────────────────────────────
SQLAlchemy==2.0.35
psycopg2-binary==2.9.9   # PostgreSQL driver (production). SQLite needs no driver.

# ── Data / ML ────────────────────────────────────────────────────────────
pandas==2.2.3
numpy==1.26.4
scikit-learn==1.5.2
scipy==1.13.1            # optional: used for KS statistic if present (falls back gracefully if not)
joblib==1.4.2
protobuf==4.25.9
ml-dtypes==0.4.1
h5py==3.11.0
tensorflow==2.17.0
tf-keras==2.17.0
# yfinance>=0.2.51
yfinance>=0.2.54,<1.0
requests==2.32.3

# ── News similarity (optional at runtime; imported lazily) ────────────────
chromadb>=0.6.0
sentence-transformers==3.1.1
huggingface_hub==0.25.1