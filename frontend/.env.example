# Base URL of the FastAPI backend. Leave unset to keep using mock data.
VITE_API_BASE_URL=http://localhost:8000/api

# When true, the app calls the real API instead of the built-in mock data layer.
VITE_USE_MOCK_DATA=false

